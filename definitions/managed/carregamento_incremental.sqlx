config {
    type: 'incremental',
    schema: 'managed',
    name: 'GF_vendas_incremental',
    tags: ['inicial', 'incremental'],
    dependencies: ['GF_ext_vendas'],
}


SELECT *
FROM ${ref('GF_ext_vendas')} cl


${
    when(incremental(),
    `WHERE pedido_data > (SELECT MAX(pedido_data) FROM ${self()})`)
}
